// App configuration values used by the mobile client.
// Replace the placeholder values with real production settings.
export const config = {
  // Use https and a real domain for production builds.
  apiBaseUrl: 'http://localhost:4000',
  googleWebClientId: 'REPLACE_WITH_GOOGLE_WEB_CLIENT_ID',
};
